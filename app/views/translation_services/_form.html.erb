<%= form_for(@translation_service) do |f| %>
  <% if @translation_service.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@translation_service.errors.count, "error") %> prohibited this translation_service from being saved:</h2>

      <ul>
      <% @translation_service.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :url %><br>
    <%= f.text_field :url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
