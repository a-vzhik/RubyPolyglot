<%= form_for(@term_request) do |f| %>
  <% if @term_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@term_request.errors.count, "error") %> prohibited this term_request from being saved:</h2>

      <ul>
      <% @term_request.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :datetime %><br>
    <%= f.datetime_select :datetime %>
  </div>
  <div class="field">
    <%= f.label :term %><br>
    <%= f.text_field :term %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
